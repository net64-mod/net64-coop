image: Visual Studio 2017

platform:
  - x64
 
configuration:
  - Release

before_build:
    - cmake -G "Visual Studio 15 2017 Win64" . -DBUILD_QT_FRONTEND=OFF

build:
  project: $(APPVEYOR_BUILD_FOLDER)\$(APPVEYOR_PROJECT_NAME).sln
