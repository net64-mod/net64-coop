language: cpp
dist: trusty
sudo: required
compiler:
- gcc
os:
- linux
install:
- sudo add-apt-repository --yes ppa:jonathonf/gcc-7.1
- sudo apt-get update
- sudo apt-get install -y g++-7
before_script:
- mkdir build
- cd build
- cmake ..
script: make
